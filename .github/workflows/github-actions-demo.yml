name: GitHub Actions Demo
run-name: is testing out GitHub Actions 🚀
on:
  workflow_dispatch:  # This enables the manual button
    inputs:
      debug_mode:
        description: 'Run with extra logs?'
        default: 'false'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch: ["amd64"]
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' 

      - name: Install npm tool
        run: npm install -g obj2gltf

      - name: Run Bash script
        run: |
          chmod +x ./scripts/downloadBaseAssets.sh
          ./scripts/downloadBaseAssets.sh
        shell: bash

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish
        uses: home-assistant/builder@2025.11.0
        with:
          args: |
            --${{ matrix.arch }}  \
            --target /data \
            --docker-hub "ghcr.io/${{ github.repository_owner }}" \
            --image ha-floorplan-3d \
            --addon
            --verion 
